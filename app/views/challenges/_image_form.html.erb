<%= form_for(@challenge) do |f| %>
  <% if @challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @challenge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <h2>Add an image for your challenge.</h2>


   <div class="field">
    <%= f.label :challengepic, "Upload a picture" %><br />
    <%= f.file_field :challengepic %>
  </div>  
 
 
  <div class="actions">

    <%= f.submit %>
  </div>
<% end %>
